server:
  port: 8989
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/orange_blog?useSSL=false&serverTimezone=Asia/Shanghai #上线把localhost改为服务器ip
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  redis:
    host: localhost #上线把localhost改为服务器ip
    port: 6379
  mail:
    host: smtp.qq.com
    username: 2814964382@qq.com
    password: gshieyrsdynhdfif  # 这里应该是您的授权码，而不是密码
    port: 465  # 使用SSL时的端口号
    properties:
      mail.smtp.auth: true  # 启用身份验证
      mail.smtp.socketFactory.class: javax.net.ssl.SSLSocketFactory  # 使用SSL Socket Factory
      mail.smtp.socketFactory.fallback: false  # SSL连接失败时不使用其他协议
      mail.smtp.ssl.enable: true  # 启用SSL
    default-encoding: UTF-8  # 设置默认编码
  mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0
      logic-delete-field: delFlag
      id-type: auto
